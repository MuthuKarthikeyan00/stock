const path = require('path');

module.exports = {
  'config': path.resolve('src/config/config.json'),    // Path to your config.json
  'migrations-path': path.resolve('src/migrations'),    // Path to your migrations
  'models-path': path.resolve('src/models'),            // Path to your models
  'seeders-path': path.resolve('src/seeders'),          // Path to your seeders (if applicable)
};
